rootProject.name = 'toolkit-root'

include(
    ':toolkit',
    ':toolkit:cross-compile--java-common',
    ':toolkit:cross-compile--java-9-gte',
    ':classpath',
    ':testkit',
    ':build-logic',
)
