allprojects {
    pluginManager.withPlugin('java') {
        apply plugin: 'name.remal.lombok'

        lombok {
            delombok {
                format {
                    pretty = true
                }
            }
        }
    }
}
